apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: "matching-deployment"
  namespace: "default"
  labels:
    app: "matching-deployment"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "matching-deployment"
  template:
    metadata:
      labels:
        app: "matching-deployment"
    spec:
      containers:
      - name: "matching-service-sha256-1"
        image: # Add the missing image here
        resources:
          requests:
            memory: "1Gi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        env:
        - name: # Add the missing env variables here
          valueFrom:
            configMapKeyRef:
              key: # Add the missing key here
              name: "matching-deployment-config-jhye"
