interface CodeChangePayload {
  sessionId: string;
  questionId: string;
  code: string;
  language: string;
}

interface SubmissionPayload {
  sessionId: string;
  questionId: string;
  code: string;
  language: string;
}