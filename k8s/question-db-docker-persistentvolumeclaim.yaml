apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: question-db-docker
  name: question-db-docker
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
