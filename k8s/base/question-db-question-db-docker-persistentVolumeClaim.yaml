#! question-db-question-db-docker-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: question-db-question-db-docker
    namespace: code
    labels:
        com.docker.compose.service: question-db
        com.docker.compose.volume: question-db-docker
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
