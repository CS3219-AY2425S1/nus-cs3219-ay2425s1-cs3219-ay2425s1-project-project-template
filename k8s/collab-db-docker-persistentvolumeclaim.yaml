apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: collab-db-docker
  name: collab-db-docker
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
