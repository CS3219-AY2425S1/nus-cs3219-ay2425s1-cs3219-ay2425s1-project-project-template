#! user-db-user-db-docker-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: user-db-user-db-docker
    namespace: code
    labels:
        com.docker.compose.service: user-db
        com.docker.compose.volume: user-db-docker
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
