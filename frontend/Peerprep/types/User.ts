export type User = {
    displayName?: string;
    email: string;
    uid: string;
    admin: boolean;
}

export type UserNet = {
    display_name: string;
    email: string;
    uid: string;
    admin: boolean;
}
